// import { Prop, Schema } from "@nestjs/mongoose";
// import { Document } from "mongoose";
// import { User } from "src/features/user/schemas/user.schema";
// import { createSchemaForClassWithMethods } from "../../../shared/mongoose/create-schema";
// import { ObjectId } from "src/shared/mongoose/object-id";

// @Schema({ timestamps: true })
// export class Inventories extends Document {
//   @Prop({ type: ObjectId, ref: User.name })
//   creator: User;

//   @Prop({ type: ObjectId, ref: User.name })
//   product: User;
// }

// export const InventoriesSchema = createSchemaForClassWithMethods(Inventories);
